# Optional: when the API is on another origin (production SPA + separate API host).
# Must include the /api path if your backend mounts routes at /api/
# VITE_API_BASE_URL=https://api.example.com/api
